/// A type with a single value. This is useful for functions that return nothing; it tends to play nicer in the type system than `void`.
const Unit unit = Unit._();

/// A type with a single value. This is useful for functions that return nothing; it tends to play nicer in the type system than `void`.
class Unit {
  const Unit._();
}
